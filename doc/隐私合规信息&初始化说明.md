# 隐私合规信息说明

> 融合SDK隐私政策：https://www.csjplatform.com/terms/gromoresdk-privacy 
> 穿山甲广告SDK隐私政策：https://www.csjplatform.com/privacy

接入事例
V>=5.3.6.1支持一键复制实现融合基础功能实现
```
TTAdSdk.init(this, new TTAdConfig.Builder().appId("xxxxxx")
                .customController(getTTCustomController()) // 隐私合规设置
                .build()
             );

//函数返回值表示隐私开关开启状态，未重写函数使用默认值
private static TTCustomController getTTCustomController(){
        return new TTCustomController() {

            @Override
            public boolean isCanUseWifiState() {
                return super.isCanUseWifiState();
            }

            @Override
            public String getMacAddress() {
                return super.getMacAddress();
            }

            @Override
            public boolean isCanUseWriteExternal() {
                return super.isCanUseWriteExternal();
            }

            @Override
            public String getDevOaid() {
                return super.getDevOaid();
            }

            @Override
            public boolean isCanUseAndroidId() {
                return super.isCanUseAndroidId();
            }

            @Override
            public String getAndroidId() {
                return super.getAndroidId();
            }

            @Override
            public MediationPrivacyConfig getMediationPrivacyConfig() {
                return new MediationPrivacyConfig() {

                    @Override
                    public boolean isLimitPersonalAds() {
                        return super.isLimitPersonalAds();
                    }

                    @Override
                    public boolean isProgrammaticRecommend() {
                        return super.isProgrammaticRecommend();
                    }
                };
            }
    }
```

### TTCustomController说明

隐私设置类，通过TTAdConfig的customController(TTCustomController var1)来设置

| 方法名  | 说明    | 
|---------------------|-----------------------|
| boolean isCanUseLocation()    | 是否允许SDK主动使用地理位置信息     |
| LocationProvider getTTLocation() | 开发者可传入自定义地理位置信息    |
| boolean alist()                | 是否允许SDK上报手机APP安装列表      | 
| boolean isCanUsePhoneState()  | 是否允许SDK主动获取手机硬件参数    |
| String getDevImei()   | 当`isCanUsePhoneState=false`时，可传入IMEI信息 |
| `boolean isCanUseWriteExternal() | 是否允许SDK使用`WRITE_EXTERNAL_STORAGE`权限   | 
| String getDevOaid()            | 开发者可传入OAID信息   |
| boolean isCanUseAndroidId()    | 是否允许SDK获取Android ID  |
| String getAndroidId()          | 开发者可传入自定义Android ID    | 
| MediationPrivacyConfig getMediationPrivacyConfig() | 获取聚合隐私控制配置对象（需开发者配置） | 

### MediationPrivacyConfig说明
聚合设置隐私类

| 方法名  | 说明    | 
|---------------------|-----------------------|
| List getCustomAppList()   | 开发者可以传入app列表     |
| List getCustomDevImeis() | 开发者可以传入多个imei值    |
| boolean isCanUseOaid()               | 是否可以使用oaid     | 
| boolean isCanUsePhoneState()  | 是否限制个性化推荐接口    |
| boolean isProgrammaticRecommend() | 是否启用程序化广告推荐 true启用 false不启用  | 

### SDK初始化说明

> 注意事项

1. useMediation仅可设置一次，不支持后续二次修改。
2. useMediation(false)默认关闭聚合功能，若useMediation设置为false，默认不使用聚合，仅使用穿山甲SDK功能。
3. 默认仅支持初始化SDK一次，多次初始化SDK以第一次初始化为准。
4. TTAdSdk中IMediationManager getMediationManager() 需确保SDK初始化完成后调用，否则可能为空。
5. V>=5.3.5.0,SDK初始化成功回调转到了子线程，请不要直接在回调里做UI操作。
6. appId为必填内容，若appid是通过服务端下发的，那么在初始化前需要做不为空的判断。
7. 开发者需确保在success回调之后再去请求广告。
8. SDK初始化API：该API必须在主线程中调用，SDK会将初始化操作放在子线程执行。

接入事例
```
public class DemoApplication extends Application {
  public static String PROCESS_NAME_XXXX = "process_name_xxxx";

  @Override
  public void onCreate() {
    super.onCreate();
    //强烈建议在应用对应的Application#onCreate()方法中调用，避免出现content为null的异常
    // TTAdsdk.init仅进行初始化,不会获取个人信息,如果要展示广告,需要再调用TTAdSdk.start方法
    TTAdSdk.init(context, buildConfig(context));
    //setp1.2：启动SDK
   

    TTAdSdk.start(new TTAdSdk.Callback() {
      @Override
      public void success() {

        Log.i(TAG, "success: " + TTAdSdk.isSdkReady());
        startActivity(context);
      }

      @Override
      public void fail(int code, String msg) {
        Log.i(TAG, "fail:  code = " + code + " msg = " + msg);
      }
      private static TTAdConfig buildConfig(Context context) {
        return new TTAdConfig.Builder()
          .appId("xxxxxx")//应用ID
          .useMediation(true)//开启聚合功能，默认false
          .supportMultiProcess(true)//开启多进程
          .build();
      }

    }
  }
 }
```

### TTAdSdk.Callback说明

| 方法名  | 说明    | 
|---------------------|-----------------------|
| void success()    | SDK初始化成功回调    |
| void fail(int errorcode, String errorMsg)   | SDK初始化失败回调   |

SDK初始化失败回调错误码说明

| value  | 说明    |
|--------|---------|
| 4201   | <ol type="1"><li>需要依赖 appcompat-v7 库：<br>`implementation "com.android.support:appcompat-v7:28.0.0"`</li><li>检查项目的 CPU 架构（SDK 默认支持 armeabi-v7a、arm64-v8a）</li><li>AndroidX 项目需检查：<br>• androidx 配置<br>• gradle 配置</li><li>若排查无问题，需提供抓包信息：<br>[穿山甲抓包指南](https://www.csjplatform.com/support/doc/5fd6ca37c09662001ce316cd)</li></ol> |

### TTAdConfig.Builder说明

| 方法名  | 说明    | 
|---------------------|-----------------------|
| appId(String var1)   | 必选参数，设置应用ID    |
|
| appName(String var1)                     | **可选参数**，设置应用名称                                                               |
| paid(boolean var1)                       | 标识是否为计费用户                                                                       |
| keywords(String var1)                    | 设置用户画像关键词列表（多个关键词用逗号分隔）                                           |
| titleBarTheme(int var1)                  | 设置广告落地页主题样式                                                                   |
| allowShowNotify(boolean var1)            | 控制是否允许SDK弹出系统通知（默认允许）                                                  |
| debug(boolean var1)                      | 开启调试模式（日志输出/测试广告）                                                        |
| directDownloadNetworkType(int... var1)   | 设置允许直接下载广告的网络类型集合（需传入网络类型常量值）                               |
| useTextureView(boolean var1)             | 控制是否使用TextureView播放视频（**62XX+版本已下线**）                                   |
| supportMultiProcess(boolean var1)        | **V≥5.1.6.0** 启用多进程模式，需配置ADN Activity属性：<br>android:multiprocess="true"   |
| setAgeGroup(int var1)                    | 设置用户年龄段（默认值：成年）                                                           |
| customController(TTCustomController var1` | 设置隐私信息控制开关（需配合TTCustomController使用）                                     |
| useMediation(boolean var1)               | **聚合广告必开** 启用聚合功能（默认关闭）                                                |
| setMediationConfig(MediationConfig var1) | 配置聚合广告特有参数                                                                     |
| themeStatus(int var1)                    | 设置主题模式：<br>`0`-正常模式 / `1`-夜间模式<br>**注意：需在初始化成功后调用才能生效**   |

### MediationConfig.Builder说明
| 方法名  | 说明    | 
|---------------------|-----------------------|
| setPublisherDid(String publisherDid)                  | **可选参数**，设置用户自定义设备ID                                                        |
| setOpenAdnTest(boolean openAdnTest)                   | **可选参数**，开启第三方ADN测试模式（默认`false`）<br>• 调试时每次重新拉取最新配置<br>• Release包必须关闭 |
| setMediationConfigUserInfoForSegment(MediationConfigUserInfoForSegment infoForSegment)` | **可选参数**，设置流量分组信息<br>**校验规则**：<br>`[A-Za-z0-9-_]{1,100}`<br>（无效值会被过滤） |
| setLocalExtra(Map<String, Object> localExtra)        | 初始化时传入自定义参数（透传给Adapter初始化对象）                                          |
| setHttps(boolean isUseHttps)                          | 设置是否支持HTTPS（**百度专用**，默认`false`）                                             |
| setCustomLocalConfig(JSONObject customLocalConfig)    | 设置兜底配置（当其他配置失效时生效）                                                       |
| setOpensdkVer(String opensdkVer)                      | 设置openSDK版本（用于判断微信小程序广告兼容性）                                            |
| setWxInstalled(boolean wxInstalled)                   | 设置微信安装状态（影响微信小程序广告展示能力）                                             |
| setSupportH265(boolean supportH265)                   | 设置是否支持H265视频格式                                                                  |
| setSupportSplashZoomout(boolean supportSplashZoomout) | 设置是否支持开屏V+广告（增强型开屏广告）                                                   |
| setWxAppId(String wxAppId)                            | 设置微信AppID（微信小程序广告必需参数）                                                    |